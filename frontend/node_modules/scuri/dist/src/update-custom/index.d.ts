import { Rule, SchematicContext } from '@angular-devkit/schematics';
import { ClassTemplateData } from '../types';
export declare type Options = {
    name: string;
    classTemplate: string;
};
export declare function updateCustom(o: Options): Rule;
export declare function updateWithCustomTemplate(templateData: ClassTemplateData & {
    templateContents: string;
    specFileContents: string;
}, context: SchematicContext): {
    position: number;
    content: string;
}[];
