import { Rule } from '@angular-devkit/schematics';
export declare class AutoSpyOptions {
    for: 'jasmine' | 'jest';
    legacy?: boolean;
    path: string;
}
export default function (options: AutoSpyOptions): Rule;
