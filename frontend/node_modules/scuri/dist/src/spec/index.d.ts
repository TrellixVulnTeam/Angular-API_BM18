import { Rule } from '@angular-devkit/schematics';
declare class SpecOptions {
    name: string;
    update?: boolean;
    classTemplate?: string;
    functionTemplate?: string;
    config?: string;
}
export declare function spec({ name, update, classTemplate, functionTemplate, config }: SpecOptions): Rule;
export {};
